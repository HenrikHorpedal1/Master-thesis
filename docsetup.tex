\usepackage{longtable}
\usepackage{pdfpages}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{amssymb}
\usepackage{mathrsfs}
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{color}
\usepackage[Lenny]{fncychap}
\usepackage[bookmarks=true]{hyperref}
\hypersetup{
	colorlinks,%
	citecolor=black,%
	filecolor=black,%
	linkcolor=black,%
	urlcolor=black
}
\usepackage{svg} % allows inclusion of SVGs
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}
\usepackage{fancyhdr}
\usepackage{times}
%\usepackage[intoc]{nomencl}
%\renewcommand{\nomname}{List of Abbreviations}
%\makenomenclature
\usepackage{natbib}
\usepackage{float}
\usepackage{booktabs}
\usepackage{nicefrac}
\usepackage{gensymb}
\usepackage{underscore} % makes _ safe outside math mode (fixes Inkscape-exported SVG labels)

\usepackage{fvextra}
\immediate\write18{mkdir -p build}% ensure minted cache dir exists on Overleaf too
\usepackage[outputdir=build]{minted}
\usemintedstyle{friendly}
\setminted{fontsize=\small, linenos, frame=lines, framesep=2mm, breaklines}
\setmintedinline{bgcolor=gray!10, breaklines, breakanywhere, fontsize=\normalsize}
\providecommand*{\listingautorefname}{Listing}

\usepackage{algorithm}
\usepackage{algpseudocode}
\newcommand{\algorithmautorefname}{Algorithm}


\usepackage{tikz}
\usepackage{circuitikz}
\usetikzlibrary{calc, decorations.pathmorphing, decorations.markings, arrows.meta}

\tikzset{>=latex}

\colorlet{discfill}{blue!8}
\colorlet{myblue}{blue!70!black}

% Spring / damper / belt styles
\tikzset{
	spring/.style = {
			line width=0.85pt,
			decorate,
			decoration={zigzag, amplitude=4pt, segment length=7pt,
					pre length=5pt, post length=5pt}
		},
	damper/.style = {
			thick,
			decoration={
					markings,
					mark connection node=dmp,
					mark=at position 0.5 with {
							\node (dmp) [thick, inner sep=0pt, transform shape, rotate=-90,
								minimum width=25pt, minimum height=5pt, draw=none] {};
							\draw [thick] ($(dmp.north east)+(2pt,0)$) -- (dmp.south east)
							-- (dmp.south west) -- ($(dmp.north west)+(2pt,0)$);
							\draw [thick] ($(dmp.north)+(0,-8pt)$) -- ($(dmp.north)+(0,8pt)$);
						}
				},
			decorate
		},
	belt/.style   = {line width=0.75pt},
	disc/.style   = {fill=discfill, line width=1.1pt},
	force/.style  = {->, line width=0.95pt},
	disp/.style   = {<-, line width=1.2pt},
}

%\floatstyle{boxed}
\floatstyle{plain}
\restylefloat{figure}

% ShareLaTeX does not support glossaries now. Sorry...
%\usepackage[number=none]{glossary}
%\makeglossary
%\newglossarytype[abr]{abbr}{abt}{abl}
%\newglossarytype[alg]{acronyms}{acr}{acn}
%\newcommand{\abbrname}{Abbreviations} 
%\newcommand{\shortabbrname}{Abbreviations}
%%\makeabbr
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

\renewcommand*\contentsname{Table of Contents}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\chaptermark}[1]{\markboth{\chaptername\ \thechapter.\ #1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
\renewcommand{\headrulewidth}{0.1ex}
\renewcommand{\footrulewidth}{0.1ex}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[LE,RO]{\thepage}\renewcommand{\headrulewidth}{0ex}}
